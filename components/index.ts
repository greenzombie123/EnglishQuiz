import "./Lesson.ts";



export const startLesson = () => {
    // get reference for lesson component
    // call getLesson to get slides
    // pass slides to component
    // render slides
};

const getLessonComponent = ()=> document.getElementById("lesson-slider") as HTMLElement

const getLesson = () => {};
